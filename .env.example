# Server
PORT=5000

# Supabase – use your existing project; invoices load from your table and stay in sync
# Project Settings → API: set SUPABASE_URL and SUPABASE_SERVICE_KEY (or SUPABASE_ANON_KEY).
# SUPABASE_URL=https://xxxxx.supabase.co
# SUPABASE_SERVICE_KEY=eyJ...
# If your table has one column per field (like the CSV), set SUPABASE_INVOICES_WIDE=true
# If your table name isn't "invoices", set SUPABASE_INVOICES_TABLE=your_table_name

# Stripe (https://dashboard.stripe.com/apikeys)
# Use test keys for development.
STRIPE_SECRET_KEY=sk_test_...

# PayPal (https://developer.paypal.com/dashboard/applications/sandbox)
# Sandbox credentials for testing; set PAYPAL_SANDBOX=false for live.
PAYPAL_CLIENT_ID=...
PAYPAL_CLIENT_SECRET=...
PAYPAL_SANDBOX=true

# Optional: Payment demand letter PDF sent with each invoice.
# Set to a public URL (e.g. Google Drive "Anyone with link" PDF), or leave unset and place server/assets/payment-demand.pdf in the repo.
# PAYMENT_DEMAND_LETTER_URL=https://drive.google.com/uc?export=download&id=YOUR_FILE_ID

# Resend (https://resend.com) – for sending invoice emails and automations
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=info@torchlinegroup.com

# Public URL of your app (for links in emails and PayPal return). Leave blank for localhost.
# APP_URL=https://your-app.railway.app

# ElevenLabs – for "Have AI call customer" (optional: you can set the API key in Settings instead)
# Create an agent and link a Twilio number in the ElevenLabs dashboard, then set:
# ELEVENLABS_API_KEY=...
# ELEVENLABS_AGENT_ID=...
# ELEVENLABS_PHONE_NUMBER_ID=...
